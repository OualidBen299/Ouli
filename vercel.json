{
  "builds": [
    { "src": "api/app.py", "use": "@vercel/python" }
  ],
  "functions": {
    "api/*": {
      "memory": 512,
      "maxDuration": 10
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/api/app.py" }
  ]
}
